#include <bits/stdc++.h>
using namespace std;
int main()
{
    int z;
    cin >> z;
    int a, b;
    while (z--)
    {
        int sum = 0;
        cin >> a >> b;
        sum = a + b;
        cout << sum << "\n";
    }
    return 0;
}