#include <bits/stdc++.h>
using namespace std;
int main()
{
    int g;
    cin >> g;
    while (g--)
    {
        int m, n, o;
        cin >> m >> n;
        o = (10 * m) + (90 * n);
        cout << o << endl;
    }
    return 0;
}